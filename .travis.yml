dist: xenial
sudo: required
services:
- docker
language: go
go:
  - "1.13.1"
addons:
  firefox: latest
cache:
  directories:
    - ${HOME}/cached-deps
env:
  global:
    - PATH=$HOME/cached-deps:$PATH

install:
- ./etc/ci/install.sh
before_script:
- ./etc/ci/setup.sh
script:
- exit 0
